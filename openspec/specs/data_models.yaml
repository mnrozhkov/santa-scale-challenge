version: 0.1.0
models:
  KidProfile:
    id: string
    name: string
    age: integer
    wishlist: string[]

  GiftRecommendation:
    id: string
    kid_id: string
    gifts: string[]
    rationale: string
    model_version: string

  Wish:
    id: string
    kid_id: string
    text: string
    model_version: string

  CardImage:
    id: string
    kid_id: string
    image_url: string
    model_version: string

  GiftCard:
    id: string
    kid_id: string
    recommendation_id: string
    wish_id: string
    image_id: string
    rendered_url: string
    status: string
