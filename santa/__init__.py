"""Santa Scale Challenge — personalized gift cards on Nebius Serverless AI.

One package, three runtimes: the ``santa`` CLI on a laptop, ``santa-service`` on a
CPU endpoint, and ``python -m santa.job`` inside GPU Jobs.
"""

__version__ = "2.0.0a0"
