# Tool-calling bench

Not run: `TOKEN_FACTORY_API_KEY` is not set.

Keep `openai/gpt-oss-120b` as `roles.llm.model` in `config/models.yaml` until this script is run against Token Factory. It will print the winner to put in yaml (do not invent one).
